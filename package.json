{
  "name": "digitalisierungsplanung-state-tool",
  "private": true,
  "type": "commonjs",
  "scripts": {
    "build:index": "node scripts/build-index.mjs",
    "build:pwa-assets": "node scripts/build-pwa-assets.mjs",
    "build:site": "node scripts/stage-static-site.mjs",
    "build:workspace-core": "node scripts/embed-workspace-core.mjs",
    "check": "npm run check:workspace-core && npm run check:repository && npm run check:index",
    "check:index": "node scripts/check-generated-index.mjs",
    "check:repository": "node --test tests/release-hygiene.test.js",
    "check:workspace-core": "node scripts/embed-workspace-core.mjs --check",
    "server:start": "node server/server.js",
    "pilot:backup": "node server/pilot-backup.js",
    "server:smoke:wss": "node server/wss-smoke.js",
    "server:smoke:wss:prod": "node server/wss-smoke.js --force-ip=198.199.83.43",
    "server:smoke:emit": "node server/emit-smoke.js",
    "server:smoke:emit:prod": "node server/emit-smoke.js --force-ip=198.199.83.43",
    "test": "npm run test:server && playwright test --grep @smoke --reporter=list",
    "test:server": "node --test server/*.test.js shared/*.test.js",
    "test:pilot": "node --test server/pilot-api.test.js",
    "test:browser": "playwright test",
    "test:webkit": "playwright test tests/webkit-product-smoke.spec.js --browser=webkit --workers=1 --reporter=list",
    "test:contracts": "playwright test tests/core-contracts.spec.js --reporter=list",
    "test:state-explorer": "playwright test tests/state-tool.spec.js --grep \"drops state explorer presets|stores reusable states|adds and uses state explorer presets|updates, deletes|reuses state explorer presets|keeps state editor focus\" --reporter=list",
    "test:state-render": "playwright test tests/core-contracts.spec.js --grep \"component data rendering|repeat sources|data wires drive|state editor exposes|state data card|global-state json paths|global state json tree|repeat over\" --reporter=list && playwright test tests/state-tool.spec.js --grep \"renders state data defaults|state data defaults flow|inspects JSON endpoints|keeps data-wire paths|shows outgoing transition buttons\" --reporter=list",
    "test:full": "npm run test:server && npm run test:browser",
    "test:ci": "npm run check && npm run test:server && npm run test:browser && npm run test:webkit",
    "test:headed": "playwright test --headed",
    "test:ui": "playwright test --ui",
    "mcp:state": "node mcp/state-blueprint-server.js"
  },
  "devDependencies": {
    "@playwright/test": "^1.52.0"
  },
  "dependencies": {
    "parse5": "6.0.1",
    "ws": "^8.21.0"
  }
}
